import { BothItemSlidingDirective } from './both-item-sliding.directive';

describe('BothItemSlidingDirective', () => {
  it('should create an instance', () => {
    const directive = BothItemSlidingDirective;
    expect(directive).toBeTruthy();
  });
});
